[project]
name = "modal-devin"
version = "0.1.0"
description = "Run Devin Outposts workers on Modal"
readme = "README.md"
requires-python = ">=3.12"
license = "MIT"
license-files = ["LICENSE"]
authors = [
    { name = "modal-devin contributors" },
]
keywords = ["devin", "modal", "outposts", "workers"]
classifiers = [
    "Development Status :: 3 - Alpha",
    "Environment :: Console",
    "Intended Audience :: Developers",
    "License :: OSI Approved :: MIT License",
    "Programming Language :: Python :: 3",
    "Programming Language :: Python :: 3.12",
    "Typing :: Typed",
]
dependencies = [
    "modal>=1.0",
    "cyclopts>=3",
    "rich>=13.6",
]

[project.urls]
Repository = "https://github.com/aaazzam/modal-devin"
Issues = "https://github.com/aaazzam/modal-devin/issues"

[project.scripts]
modal-devin = "modal_devin.cli:run"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["modal_devin"]

[dependency-groups]
dev = [
    "pytest>=9.1.1",
    "ruff>=0.15.20",
    "ty>=0.0.57",
]

[tool.pytest.ini_options]
testpaths = ["tests"]

[tool.ruff]
line-length = 100
target-version = "py312"

[tool.ruff.lint]
select = ["B", "E", "F", "I", "UP"]
